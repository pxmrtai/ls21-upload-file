extends ../layouts/common 
block headContent
block navbar

 h1 user

 form.d-flex(action="/users/index", method="POST")
   .from-group
      label(for="nameUser") Name
      input#nameUser.form-control(type="text",name='name', required="required")
      button.btn.btn-primary make
 

 ul 
  each user in userList
   li
    span= user.name
    a(href="/users/" + user.id) view 
    div
    a(href="/users/index/" + user.id+"/delete") delete
  
   
   
  div
  div
  div
 a(href="/" class="btn btn-primary" role="button") Menu List
   
   
   
   
          